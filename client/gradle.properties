org.gradle.jvmargs=-Xmx2g
android.useAndroidX=false
android.nonTransitiveRClass=true

# Version of both apps (service and settings). Keep in sync with the workspace version in
# Cargo.toml; releases are tagged v<version>.
ftbridgeVersion=1.2.0
